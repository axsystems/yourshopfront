# Session Close-Out — 2026-05-24

Mega-shipping day. 16 PRs merged to `master`, 1 draft PR awaiting operator review, 1 background agent landing tomorrow.

Master HEAD on close-out: `f303329`.

---

## What shipped (merged to master)

In merge order:

| PR | Type | Summary |
|---|---|---|
| #21 | feat(dashboard) | Stream A — Supabase Auth foundation |
| #22 | feat(dashboard) | Stream B — app shell + Home |
| #23 | feat(dashboard) | Stream C — edit-requests CRUD + threaded comments |
| #24 | feat(dashboard) | Stream D — billing page + deep-link Portal flows |
| #25 | chore(security) | 7 audit-driven hardening fixes (MED-A1/A2/A3, MED-C1/C3, LOW-C2, VULN-M1) |
| #26 | chore(cleanup) | Response shape normalization + `getCustomerById` helper + dedupe migration 0011 |
| #27 | chore(polish) | A11y touch targets + focus ring + `.gitignore` additions + `@vercel/analytics` installed |
| #28 | fix(mobile) | Demo-card skeleton no longer covers OG image on mobile + operator SMS via Quo on Stripe webhook |
| #29 | fix(copy) | Removed Google Business Profile management feature claim (6 surfaces) |
| #30 | fix(lint) | Unblock CI — `react/no-unescaped-entities` + `react-hooks/set-state-in-effect` |
| #31 | fix(checkout) | Default subscription tier to launch promo to plug ad-funnel leak |
| #32 | feat(analytics) | GA4 (`G-W1VNYD94V9`) + Google Ads conversion scaffolding (GA4 live; Ads still needs operator config) |
| #33 | fix(pricing) | Demo + home now display launch promo (was showing full price → bounce) |
| #35 | fix(pricing) | Strikethrough original price next to launch promo ($299 → $99 visible) |
| #36 | fix(stripe) | **URGENT** — omit `allow_promotion_codes` when discounts set (subscription checkout was 400-ing for all customers) |

## Awaiting operator review

- **#34 DRAFT** — `feat(demos)` Phase A: hero images for all 30 themes. Preview: <https://yourshopfront-git-feat-demos-phase-a-82f9ca-axsystems-projects.vercel.app>

## Running in background (lands tomorrow)

- **Phase B** — services/gallery/about/CTA redesign for all 30 demos. Autonomous agent in a separate `wt.exe` tab. Scope avoids hero files so it won't conflict with Phase A.

---

## Supabase migrations applied to production today

Applied via Supabase Management API:

- `0008_ai_copy_state.sql`
- `0009_auth_customer_link.sql`
- `0010_edit_requests.sql`
- `0011_dedupe_auth_user_id_idx.sql`
- `0012_edit_request_append_comment.sql`

## Vercel environment variables added (Production)

- `NEXT_PUBLIC_SUPABASE_URL` — dashboard auth
- `NEXT_PUBLIC_GA4_ID = G-W1VNYD94V9` — GA4 tracking
- `QUO_API_KEY` — operator SMS (shared with sellyourhomenowaz)
- `QUO_FROM_NUMBER = +16234398208`
- `QUO_OPERATOR_PHONE = +16236920494`

## Third-party config done by operator today

- Stripe Customer Portal configured (Dashboard → Settings → Billing)
- Supabase Auth fully set up (Email provider + URLs + SMTP via Resend + Magic Link template)
- Resend Pro upgrade ($20/mo)

---

## Pending operator actions

1. **Review + merge PR #34** (Phase A hero images)
2. **Review Phase B PR** when background agent lands it
3. **Google Ads conversion setup**:
   - Create conversion action in Google Ads UI
   - Add `NEXT_PUBLIC_GOOGLE_ADS_ID` (format `AW-XXXXXXXXX`) to Vercel Production
   - Add `NEXT_PUBLIC_GOOGLE_ADS_CONVERSION_LABEL` to Vercel Production
   - Manually redeploy (env-only changes don't auto-trigger)
   - Smoke: complete a real checkout, verify conversion fires in Google Ads diagnostics
4. **Optional**: hero strapline strikethrough text ("(was $299)") — small CSS-only follow-up
5. **Review this docs PR** + merge

---

## Stack additions this session

- `@vercel/analytics` — installed in PR #27
- GA4 + Google Ads gtag scaffold — PR #32
- Quo SMS integration for operator alerts on new Stripe checkouts — PR #28
- Customer dashboard routes (`/login`, `/app`, `/app/edit-requests`, `/app/billing`) — PRs #21-#24
- `Theme.heroImage` field — landing with PR #34 (Phase A)

## Current pricing display (live)

- Demos + home: launch promo **$99 setup** with strikethrough **~~$299~~** visible
- Default checkout tier: subscription with `launch_promo_3mo` coupon applied
- Coupon recurrence: $50 off, 3 months → $99 → $99 → $99 → $149/mo
